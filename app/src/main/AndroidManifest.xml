<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.gameproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GameAppProject">
        <activity android:name=".CommunityActivity"/>
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".MakingCharacter" />
        <activity android:name=".Game.Start_Game_Activity"/>
        <activity android:name=".StoreAcitivity"/>
        <activity android:name=".RankingAcitivty"/>
        <activity android:name=".Game.GameMain_Activity"/>
    </application>

</manifest>